<h1>Items</h1>
<% @items.each do |item| %>
  <section id='item-<%= item.id %>'>
    <%= item.name %>
    <%= item.user.name %>
    <%= item.inventory %>
    <%= item.price %><br>
    <img class='small-item-img' src="<%= item.image %>">
  </section>
<% end %>
